services:
  next-app:
    build:
      context: ./frontend/
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  express-app:
    build:
      context: ./backend/
      dockerfile: Dockerfile
    ports:
      - 3030:3030